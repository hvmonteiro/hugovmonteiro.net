{
  "name": "hugovmonteiro.net",
  "version": "1.0.0",
  "keywords": [],
  "author": "Hugo V. Monteiro",
  "license": "ISC",
  "description": "Personal website @ https://www.hugovmonteiro.net",
  "scripts": {
    "postinstall": "hugo-installer --version 0.124.1 --extended",
    "hugo": "exec-bin bin/hugo/hugo",
    "hugo:dev": "exec-bin bin/hugo/hugo server --minify --disableFastRender --navigateToChanged --templateMetrics --templateMetricsHints --buildDrafts --buildExpired --buildFuture --watch --forceSyncStatic --verbose -D",
    "hugo:server": "exec-bin bin/hugo/hugo server --minify --disableFastRender --navigateToChanged --templateMetrics --templateMetricsHints --buildFuture --watch --forceSyncStatic",
    "hugo:build": "exec-bin bin/hugo/hugo --minify"
  },
  "dependencies": {
    "hugo-installer": "^4.0.1"
  },
  "devDependencies": {
    "exec-bin": "^1.0.0"
  }
}

